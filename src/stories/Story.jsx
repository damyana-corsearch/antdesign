import React from 'react';
import { Button, Layout, Header } from 'antd';

/** Primary UI component for user interaction */
export const Story = ({ primary, backgroundColor, size, label }) => {
  return (<>
    <Layout>
      <Header></Header>
    <Button>test test</Button>
    </Layout>
    </>
  );
};